import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "plus")
            Text("I`m eunchan")
            Text("zzzzzzzz")
        }
    }
}
